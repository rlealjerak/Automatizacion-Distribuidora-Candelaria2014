"""
Normalization (build-order step 4).

Owns: confidence-scored proposed column mapping (e.g. "UNIT PRICE" / "SALE"
/ "SALE PRICE" -> cost field), the case-quantity unit lookup table (EA, DP,
CA, ST, PCS, ...), and the owner confirmation step before a mapping is
used to process a run.

Not yet implemented.
"""

"""
Internal modules of the modular monolith.

Each submodule owns one concern and is the *only* place that concern's
logic lives — in particular, none of this logic ever moves into OpenClaw.
See the top-level README for the module -> build-order-step mapping.
"""
